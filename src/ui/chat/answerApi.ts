/** The one place the chat gets answer()/deepen() from: the engine's answer service. */
export { answer, deepen } from "../../routing/answerService";
export type { AnswerContext } from "../../routing/answer";
